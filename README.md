# 🧠 PPO на чистом NumPy — Reinforcement Learning без PyTorch и TensorFlow

Этот проект демонстрирует реализацию алгоритма **Proximal Policy Optimization (PPO)** с нуля, **на чистом NumPy**, без использования глубоких фреймворков.  
Для демонстрации используется упрощённая среда **CartPole**, реализованная вручную.

---

## 🚀 Описание проекта

В проекте представлены три основных компонента:

1. **Среда `SimpleCartPole`**  
   - Упрощённая физическая модель классической задачи "Маятник на тележке".  
   - Управление тележкой осуществляется дискретными действиями:  
     `0` — толкнуть влево, `1` — толкнуть вправо.  
   - Цель агента — удерживать шест в вертикальном положении как можно дольше.

2. **Нейросеть `ActorCriticNetwork`**  
   - Реализована **на чистом NumPy**, без PyTorch/TensorFlow.  
   - Использует архитектуру Actor-Critic:
     - `Actor` — выдаёт вероятности действий π(a|s) через Softmax.
     - `Critic` — оценивает состояние V(s).  
   - Реализованы функции прямого и обратного прохода (backpropagation) вручную.  
   - Применяется **gradient clipping** для стабилизации обучения.

3. **Алгоритм `PPOAgent`**  
   - Реализует **Proximal Policy Optimization** с клиппингом.  
   - Поддерживает:
     - Generalized Advantage Estimation (GAE)
     - Clipped Objective Function
     - Энтропийный бонус для исследования
     - Нормализацию advantage и returns  
   - Всё вычисляется **на чистом NumPy**.

---

## ⚙️ Структура проекта

